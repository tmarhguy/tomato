{
  "name": "tomato-web",
  "private": true,
  "type": "module",
  "description": "Static broadsheet for The Tomato — deploy web/ as-is to GitHub Pages or Vercel",
  "scripts": {
    "test": "node --test tests/*.mjs",
    "serve": "python3 -m http.server 8080"
  },
  "engines": {
    "node": ">=18"
  }
}
